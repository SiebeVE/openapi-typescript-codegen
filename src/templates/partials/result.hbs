{{~#if results~}}
{{#each results}}
{{#equals @root.accept 'application/ld+json'}}
JsonLd<{{>type}}{{#unless @last}} | {{/unless}}>
{{else equals @root.accept 'application/hal+json'}}
JsonHal<{{>type}}{{#unless @last}} | {{/unless}}>
{{else}}
{{>type}}{{#unless @last}} | {{/unless}}
{{/equals}}
{{/each}}
{{~else~}}
void
{{~/if~}}
